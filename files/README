Core Files

Данная система была разработана для обмена файлами между студентами внутри ВУЗ-а.

Так кк система имеет микро харамктер, то для настройки потребуется только настройка подключения к бд (в index.php и download.php) и настройка домена Core Admin в тех же файлах.

Советую закрыть доступ к папке Files с помощью .htaccess чтобы избежать хака.

В папке SQL хранится дамп базы данных.