Core Library

Это система цифровой библиотеки.

Эта система была первая из всех. Для ее настройки потребуется:
1. Заливка дмпа в бд.
2. Исправление параметров доступа к бд в /include/init.php
3. В том же файле настройка доступа к Core Admin.

Система должна заработать в штатном режиме.

В папке SQL хранится дамп базы данных.